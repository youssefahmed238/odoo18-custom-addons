<odoo>

    <!-- Inherit the account.move form view to add the petty_employee field in the lines -->
    <record id="account_move_view_inherit" model="ir.ui.view">
        <field name="name">account.move.view.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook/page[@name='aml_tab']//list/field[@name='account_id']" position="after">
                <field name="petty_employee"/>
            </xpath>
        </field>
    </record>

</odoo>